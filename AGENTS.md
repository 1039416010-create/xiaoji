# 《走地鸡王》Unity 项目开发规范

本文件适用于仓库根目录及其全部子目录。Codex 在修改本项目之前必须先阅读本文件，再阅读 `Docs/Development/README.md` 和当前阶段文档。若子目录存在更具体的 `AGENTS.md`，以更具体者为补充；任何子规则不得放宽本文件中的安全、触控、确定性和验收要求。

## 1. 项目目标与固定约束

- 引擎：Unity 6.3 LTS。
- 渲染：Universal Render Pipeline（URP）。
- 目标平台：Windows 10/11 x64，本地离线运行。
- 逻辑画面：固定按 2:1 构图，基准分辨率为 1920×960。
- 玩家人数：1–4 人；玩家只位于桌面两条长边。
- 输入设备：Windows HID 红外多点触控。
- 所有游客交互必须通过可见的 Unity UI `Button` 完成。禁止键盘快捷键、隐藏热区、滑动、拖拽、长按、双击、鼠标悬停和依赖右键的必要操作。
- 比赛：5 条互相平行、从左向右的赛道；每局恰好 1 只鸡有效冲线并成为冠军。
- 经济：每名玩家初始 50 金币；单次下注只能选 1、5、10；下注确认后不可修改。
- 公平性：所有玩家锁定下注后，`RaceDirector` 才能生成本局结果及完整受控随机剧本。下注前不得决定冠军，也不得根据玩家选择或下注额调整结果。
- 连局：冠军留场，另外 4 只鸡被替换；玩家金币跨局继承。
- 结束：所有已加入玩家金币均为 0，或达到配置的最大局数时进入 `GameOver`。

除非任务明确要求变更，以上内容均视为不可协商的产品契约。任何修改契约的提交必须同步更新本文件、配置、测试和阶段文档。

## 2. Codex 工作协议

每次任务必须遵循以下顺序：

1. 阅读本文件、阶段索引和当前阶段文档。
2. 检查工作区状态，保留用户已有修改，不覆盖无关内容。
3. 先写出本次任务涉及的文件和验收点，再实现最小闭环。
4. 一次只完成一个阶段；前一阶段未通过退出条件时，不进入后一阶段。
5. 代码、配置、场景和测试必须同一任务内保持一致。
6. 运行与改动风险相称的自动化检查，并记录无法自动验证的实体设备项目。
7. 结束时汇报：完成内容、变更文件、测试结果、遗留风险、下一阶段入口。

不得只创建类名或空方法来宣称阶段完成。无法在无 Unity 编辑器环境下验证的内容，要清楚标为“待编辑器验证”，不得伪造通过结果。

## 3. 推荐目录结构

```text
Assets/
└── _Game/
    ├── Art/
    │   ├── Characters/
    │   ├── Environment/
    │   ├── Materials/
    │   ├── UI/
    │   └── VFX/
    ├── Audio/
    │   ├── Music/
    │   ├── SFX/
    │   └── Mixers/
    ├── Config/
    │   ├── Chickens/
    │   ├── Race/
    │   ├── Economy/
    │   └── Presentation/
    ├── Prefabs/
    │   ├── Chickens/
    │   ├── Race/
    │   └── UI/
    ├── Scenes/
    ├── Scripts/
    │   ├── Runtime/
    │   │   ├── Core/
    │   │   ├── Flow/
    │   │   ├── Players/
    │   │   ├── Betting/
    │   │   ├── Race/
    │   │   ├── Chickens/
    │   │   ├── UI/
    │   │   ├── Input/
    │   │   ├── Audio/
    │   │   ├── Persistence/
    │   │   └── Diagnostics/
    │   ├── Editor/
    │   └── Tests/
    │       ├── EditMode/
    │       └── PlayMode/
    └── Settings/
Packages/
ProjectSettings/
Docs/
└── Development/
```

- 所有自有资源放在 `Assets/_Game`，不得修改包缓存。
- 每个程序集目录使用 `.asmdef` 隔离：Runtime、Editor、EditMode Tests、PlayMode Tests。
- 场景内只保留组合与引用；可复用对象必须做成 Prefab；数值规则必须配置化。
- 禁止建立 `Scripts/Managers` 垃圾桶目录。脚本按业务域归档。

## 4. 命名规范

- C# 命名空间根：`GroundChickenKing`。
- 类型、方法、属性、事件：`PascalCase`；接口以 `I` 开头。
- 私有字段：`_camelCase`；序列化私有字段仍使用 `_camelCase`。
- 局部变量与参数：`camelCase`；常量：`PascalCase`。
- 布尔命名使用 `Is`、`Has`、`Can`、`Should` 前缀。
- 配置类型：`*Config`；运行时状态：`*State`；数据快照：`*Snapshot`；事件参数：`*EventArgs`。
- Prefab：`PF_<Domain>_<Name>`；场景：`SCN_<Name>`；材质：`MAT_<Name>`；动画控制器：`AC_<Name>`；动画片段：`AN_<Name>_<Action>`；音频：`SFX_` 或 `BGM_`；ScriptableObject 实例：`CFG_<Domain>_<Name>`。
- UI GameObject：`UI_<Panel|Button|Text|Image>_<Purpose>`。按钮必须以 `UI_Button_` 开头。
- 测试名：`MethodOrState_Condition_ExpectedResult`。
- 一份主要类型对应一个同名 `.cs` 文件。禁止无意义缩写和 `Manager2`、`Temp`、`New`、`Final` 等名称。

## 5. 架构与职责边界

### GameFlowController

- 只管理有限状态机、阶段切换和顶层超时恢复。
- 标准状态：`Boot`、`MainMenu`、`PlayerJoin`、`Warmup`、`Betting`、`RaceCountdown`、`Racing`、`Settlement`、`RosterRefresh`、`GameOver`。
- 只能通过明确的命令或领域事件推进；禁止通过每帧轮询散落条件切换状态。
- 不直接扣金币、不选择冠军、不移动鸡、不读取具体触点。

### PlayerManager

- 管理 1–4 名玩家的加入位置、金币余额、破产状态和跨局会话状态。
- 提供只读快照；金币变更必须通过有原因码的原子操作。
- 不管理按钮显示，不生成比赛结果，不控制鸡。

### BettingManager

- 管理当前局每位有效玩家的选鸡、下注额、确认与锁定状态。
- 只接受 1、5、10 且不超过余额的下注。
- 全员锁定时发布一次 `AllBetsLocked`；锁定后拒绝所有更改。
- 不预扣或派彩之外的经济逻辑；结算必须防止重复执行。

### RaceDirector

- 仅在收到 `AllBetsLocked` 后生成 `RacePlan`。
- 使用显式随机种子，预生成冠军、每只鸡的运动分段、事件时间与结算顺序。
- 验证剧本：恰好一个冠军、事件不越界、持续时间受限、失败鸡不得先于冠军有效冲线。
- 运行时只播放既定剧本；不得在 `Update` 中临时抽取影响赛果的随机数。
- 不读取玩家押注目标与押注额。若为展示需要读取玩家数量，也不得影响胜者概率。

### ChickenController

- 只执行分配给自己的 `ChickenRacePlan`，驱动位移、朝向、动画与表现事件。
- 程序控制世界位置；动画不得使用 Root Motion 推进赛道距离。
- 不决定自己的速度曲线、随机事件、胜负或奖励。
- 必须支持取消、重置和强制到达安全姿态。

### UI 与其他服务

- `UIController`/各 View 只呈现状态并把按钮点击转换为命令，不持有业务真相。
- `TouchInputDiagnostics` 只做触点可视化与诊断，不绕过 EventSystem 触发游戏行为。
- `SettlementService` 根据不可变的比赛结果结算一次，并输出可审计记录。
- `RosterService` 保留冠军实例或身份，补充四名不重复的失败鸡替代者。
- `SessionService` 管理一场完整会话；默认不持久化未结束的游客金币，除非配置明确要求。

依赖方向必须由表现层指向应用层接口，再指向领域模型；领域模型不得引用 Unity UI。跨域通信优先使用明确接口和 C# 事件，避免全局静态事件总线。

## 6. ScriptableObject 配置规范

至少提供以下配置：

- `GameRulesConfig`：初始金币 50、允许下注额 `[1,5,10]`、最少/最多玩家、最大局数、状态超时。
- `RaceConfig`：5 条赛道、起终点、比赛时长范围、速度范围、事件间隔、冲线容差、脚本生成重试上限。
- `ChickenDefinition`：稳定 ID、展示名、Prefab、外观标签、体型、性格标签、动画能力，不包含固定胜率。
- `RaceEventConfig`：事件类型、权重、前置条件、持续时间、位移约束、冷却和动画键。
- `PresentationConfig`：热身、倒计时、结算、换鸡等时长及 UI 动画时间。
- `TouchConfig`：最小按钮尺寸、边缘安全区、重复点击冷却、最大并发触点。

配置必须在 `OnValidate` 或独立验证器中检查；无效配置在开发构建中应明确报错并阻止开局。运行时不得偷偷修改 ScriptableObject 资产；需要修改时复制为运行时数据。

## 7. 触控与 UI 规范

- 使用 Unity Input System 与 `InputSystemUIInputModule`，以独立 Pointer ID 支持并发触点。
- 业务代码只响应 UI `Button.onClick` 或封装后的按钮命令。
- 禁止使用 `Input.GetKey*`、`Keyboard.current`、键盘 Esc 退出、空白区域点击继续等游客流程。
- 鼠标仅可在编辑器/开发构建中作为触控仿真；发布版不得依赖鼠标悬停状态。
- 同时触摸不同玩家区域时必须互不抢占、互不取消。不得实现全局单触点锁。
- 一个触点从按下到抬起只能触发一个按钮；遮挡层必须正确管理 `raycastTarget`。
- 每个必要操作均有清晰可见按钮、按下反馈、禁用态和结果反馈。
- 推荐物理触控目标不小于约 25 mm；最终像素阈值由现场投影和触控映射实测后写入 `TouchConfig`。
- 主画布按 1920×960 设计，使用安全区而非硬编码屏幕像素。上侧两个玩家面板整体旋转 180°；中央赛道尽量使用双向可读图形。
- 四个玩家区域颜色和图标双编码，不得仅靠颜色区分。
- 必须提供屏幕内“退出游戏”“返回主菜单”“重新开始”按钮，并对破坏会话的操作提供触控确认弹窗。

## 8. 比赛、动画与受控随机

- 随机种子由 `RaceSeed` 记录；建议组合会话 ID、局号和安全随机值后生成，不得使用当前帧时间在多处各自播种。
- 同一版本、同一配置哈希、同一种子必须生成等价的冠军与事件序列。
- `RacePlan` 在倒计时开始前完成并锁定，包含版本、配置哈希、种子、冠军 ID、总时长及五份鸡剧本。
- 行为可包括冲刺、减速、停顿、摔倒、掉头、终点前停住、干扰邻道等，但所有事件必须由计划调度。
- 掉头和干扰只能改变表现与受控位移，不得造成鸡离开合法赛道或碰撞物理决定胜负。
- 冠军必须有唯一有效冲线时间；其他鸡在结算触发前不能有效穿越终点判定面。
- 必须设置比赛硬超时和故障兜底：若播放偏离计划，由 `RaceDirector` 依据计划强制完成，不得卡局。
- Animator 参数使用稳定哈希；动作状态必须可中断并回到 Locomotion/Idle。
- 禁止 Root Motion 控制赛道推进，禁止刚体碰撞决定比赛结果，禁止用动画事件直接结算经济。
- 五只鸡可以外观、体型、热身和跑姿不同，但体型碰撞不得影响赛果；首版建议使用非物理或运动学表现。

## 9. 经济、换鸡与结束规则

- 下注锁定时校验余额；扣款时机在全项目内保持一致，推荐锁定即扣款。
- 命中玩家获得“所押金币的二倍”按总返还处理：锁定已扣本金后，命中返还 `2 × stake`；未命中不返还。净结果分别为 `+stake` 与 `-stake`。
- 每笔余额变化记录 `before`、`delta`、`after`、玩家、局号和原因。
- 每局结算只能执行一次；重复事件或场景重入不得重复派彩。
- 冠军保留到下一局，其余四名离场并由新定义替换；新阵容仍为 5 只且 ID 唯一。
- 金币为 0 的玩家不可下注，继续旁观；只有所有已加入玩家均为 0 才因破产结束。
- 达到 `GameRulesConfig.MaxRounds` 后，无论余额如何均结束，并展示最终金币排名和结束原因。

## 10. 日志、诊断与可复现性

- 所有日志带分类、会话 ID、局号和帧/时间戳；禁止输出个人信息。
- 每局至少记录：阵容 ID、下注锁定摘要、种子、配置哈希、冠军、剧本验证结果、结算流水、结束条件。
- 发布版日志采用轮转或大小上限，避免长期展览填满磁盘。
- 开发构建提供仅通过可见 UI 按钮进入的诊断页，显示触点 ID/位置、分辨率、配置版本、最近种子和错误；发布配置可隐藏入口，但不得绑定键盘快捷键。
- 错误分级：可恢复警告、当前局中止、会话重置。任何恢复都必须回到可操作的 UI 状态。

## 11. 测试与质量门槛

每个功能至少包含适合的 EditMode 单元测试；跨帧、场景、按钮、动画流程使用 PlayMode 测试。核心门槛：

- 1、2、3、4 人均可加入并开始。
- 下注只接受 1/5/10，不能超过余额；确认后不可变更。
- `RacePlan` 不会在全员锁定前生成。
- 批量种子测试中每局恰好一名冠军，五只鸡始终处于各自赛道，比赛在硬超时内完成。
- 同种子和配置生成相同结果；日志足以复现失败局。
- 结算幂等，余额永不为负，跨局继承正确。
- 冠军留场、四名败者替换、阵容数量和唯一性正确。
- 全员破产和最大局数两种 GameOver 均可达；单个玩家破产不会提前结束。
- 两侧 UI 朝向正确，1920×960 下无遮挡；常见 2:1 缩放下保持布局。
- 至少 4 个并发触点在不同玩家按钮上可独立完成点击；最终必须使用实际红外 HID 设备做 10 点触控与边缘校准测试。
- Windows x64 Development Build 与 Release Build 均能启动、完整玩完多局并从 UI 退出。

任何包含编译错误、丢失引用、控制台未解释异常、无退出路径或可能卡局的阶段不得验收。

## 12. 禁止事项

- 禁止任何必要的键盘快捷键或键盘兜底交互。
- 禁止在下注锁定前生成、缓存、泄漏或暗示冠军。
- 禁止根据下注分布、玩家余额或玩家身份操纵冠军。
- 禁止在播放过程中临时随机决定影响名次的事件。
- 禁止让多个系统同时写玩家金币、游戏状态或鸡的位置。
- 禁止以碰撞、帧率或动画 Root Motion 决定胜负。
- 禁止使用 `FindObjectOfType`、字符串场景查找或无界单例作为正常依赖注入方案。
- 禁止把规则常量散落在 MonoBehaviour 中。
- 禁止每帧分配集合、LINQ 或产生高频日志；目标设备上应保持稳定帧时间。
- 禁止修改 `Library/`、`Temp/`、`Logs/`、`obj/`，也不得提交这些目录。
- 禁止引入联网、账号、真实货币、购买、提现或概率付费机制。
- 禁止用“临时跳过测试”“以后再接 UI”作为阶段完成标准。

## 13. 完成定义

一个阶段只有在以下条件全部满足时才算完成：代码可编译；资产引用完整；配置验证通过；该阶段自动化测试通过；手工验收步骤已记录；没有未解释错误；文档与行为一致；能够从上一阶段入口进入、从本阶段安全退出；已知限制被明确列出。

